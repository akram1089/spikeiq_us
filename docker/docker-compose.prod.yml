# Production overlay for VPS deployment.
# Isolated project name avoids conflicting with other Docker apps on the host.
name: spikeiq_us

services:
  clickhouse:
    container_name: spikeiq_clickhouse
    ports: !override
      # 8126 avoids conflict with other ClickHouse on host port 8123
      - "8126:8123"

  kafka:
    ports: !reset []

  postgres:
    ports: !reset []

  backend:
    ports: !reset []

  frontend:
    ports: !reset []

  nginx:
    image: nginx:1.27-alpine
    container_name: spikeiq_nginx
    restart: always
    depends_on:
      frontend:
        condition: service_started
      backend:
        condition: service_started
    ports:
      # Bind to localhost only — host nginx/Caddy proxies spikeiq.mooo.com here
      - "127.0.0.1:9080:80"
    volumes:
      - ../deploy/nginx/spikeiq.conf:/etc/nginx/conf.d/default.conf:ro
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
